# 문자열 뒤집기 (주어진 문자열의 문자 순서를 반대로 바꾸는 작업을 수행한다)
# 프로그램 구동은 메인에서 구동

def reverse_string(text):
    return text[::-1]   # 문자열의 순서를 뒤집는 함수

def to_uppercase(text):
    return text.upper()  # 대문자로 바꾸기

def to_lowercase(text):
    return text.lower()  # 소문자로 바꾸기